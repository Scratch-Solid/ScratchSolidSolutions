import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Confirm Data Deletion | Scratch Solid Solutions",
  description: "Confirm your data deletion request.",
};

export default function ConfirmDataDeletionLayout({ children }: { children: React.ReactNode }) {
  return children;
}
